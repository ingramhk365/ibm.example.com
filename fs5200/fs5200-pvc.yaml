apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fs5200-pvc
  namespace: demo
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: gold
